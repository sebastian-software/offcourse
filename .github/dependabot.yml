# Managed by Alfred/OpenClaw for sebastian-software.
# Renovate owns version-update PRs (see renovate.json); Dependabot remains enabled
# for security alerts only, with update PRs disabled to avoid overlapping bots.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "Europe/Berlin"
    open-pull-requests-limit: 0
    groups:
      github-actions-dependencies:
        patterns:
          - "*"
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "Europe/Berlin"
    open-pull-requests-limit: 0
    groups:
      npm-dependencies:
        patterns:
          - "*"
