# Codecov Configuration
# https://docs.codecov.com/docs/codecovyml-reference

coverage:
  precision: 2
  round: down
  range: "60...100"

  status:
    project:
      default:
        # Baseline after removing broad V8 ignore blocks and reporting CLI commands.
        # Raise this as the newly visible browser and state-management gaps are covered.
        target: 20%
        threshold: 1%
    patch:
      default:
        target: 70%
        threshold: 10%

# Ignore files from coverage report
ignore:
  - "src/**/*.test.ts"
  - "src/**/*.integration.test.ts"
  - "src/**/index.ts"
  - "dist/**"
  - "node_modules/**"

# Comment configuration for PRs
comment:
  layout: "reach,diff,files"
  behavior: default
  require_changes: false
  require_base: false
  require_head: true
